# 变更日志

[English](CHANGELOG.md) | **简体中文**

本项目的重要变化记录在此。版本发布遵循[语义化版本](https://semver.org/lang/zh-CN/)，发布内容以 GitHub Release 和对应 Git 标签为准。

## [未发布]

### 新增

- **机柜功率看板**（`/admin/racks`）——机柜、槽位分配、核算功率、GPU 实时功耗，并标记未上报或核算功率不足的节点。系统不预置机柜。
- **积分余额预警邮件**——在邮件设置中配置积分预警阈值；每次向下越过阈值只发送一封邮件，在事务内排队并自动重试。
- **GPU 完全不可见**——节点用户 GPU 策略除限制为部分 GPU 外，还可隐藏全部 GPU（`deny_all`）。
- **持久化节点账号动作**——创建账号与 UID/GID 对齐会持久化、按租约重试，并且只有收到节点回执才算完成（`POST /api/node/actions/:id/result`）。
- 账号开通页面新增**节点推荐面板**；新账号申请表单改为结构化字段（研究方向、主要任务、使用强度）。
- **`scripts/finish_pending_release.sh`**——一次完成备份、主控安装、commit 与迁移校验，以及主→备同步。HA 同步 worker 现在也会同步并校验迁移文件。

### 变更

- 运营看板围绕用户活动、进程时长、余额与每日趋势重新组织，并按查看者可见的节点范围统计。
- 集群状态、节点管理与界面演示完成本地化。
- 向计算节点分发工作区时不再复制 `docs/`；上一份节点部署报告会被归档而不是覆盖。
- 控制器构建会嵌入 commit、构建时间与工作区是否有改动的标记。

### 修复

- 磁盘容量告警按挂载点锁存，重复上报和重启不再产生重复的严重事件，也不再计入疑似恶意用户汇总。
- 仅日期的 `to` 过滤条件按 PostgreSQL 微秒精度包含当天全部时间。
- 排队下发的节点动作与立即下发的动作使用相同的规范化内容。
- 空的 GPU 允许列表曾在多个层面被当作“无限制”；Agent 策略状态文件损坏时现在会保持已有限制，而不是解除限制。
- 移除了界面演示中残留的部署方品牌信息。
- HA 备机不再重复发送从主控数据库同步来的余额预警邮件。

### 安全

- 节点安全事件详情与可疑账号汇总在渲染前进行 HTML 转义。此前挖矿检测采集到的进程命令行可以向管理员会话注入标记。
- “完全不可见”GPU 策略会同时携带哨兵 GPU 编号，早于 `deny_all` 的旧版 Agent 也会隐藏全部 GPU，而不是解除限制。
- GPU 策略状态文件改为以 `0640` 权限原子写入，写入中断不会再留下导致后续策略更新全部失败的状态文件。
- 统计路由拒绝超过 1830 天的日期区间。
- 节点受限的看板统计不再包含查看者不可见节点上的专属积分与发放记录。
- 运维脚本通过标准输入而非进程参数向 `curl` 传递 admin token。
- 节点动作投递令牌改为常量时间比较。

### 升级说明

- 迁移 `0081`–`0086` 会在启动时自动执行。
- 请先升级节点 Agent，再升级控制器。旧版 Agent 不会回执持久化账号动作，这些动作会在达到重试上限后失败。

## [3.2.0] — 2026-08-14

首个公开版本。版本号沿用本项目内部开发时的序列，此前没有公开发布过。

### 包含内容

- **控制器** — HTTP API、策略引擎、用量计费、启动时按顺序执行的只向前迁移、定时任务，以及同源托管 Web 界面。
- **Node Agent** — 指标采集、动作执行、CPU 与内存限制、SSH 状态上报和安全信号。
- **Web 界面** — Vue 3 与 Element Plus，构建到 `web/dist` 并由控制器托管。
- **两种部署方式** — 由 systemd 托管的源码构建，以及包含 PostgreSQL 与控制器的 `docker compose` 容器方案。
- **首次 Setup** — 集中配置平台标识、注册域名、SSH 入口、价格、用户准则、SMTP 和 HA 的向导。只要有必填就绪检查未通过，它就拒绝保存。
- **双语文档** — 英文是源语言，`docs/zh-CN/` 是完整镜像，由 CI 中的 `scripts/check_docs.sh` 强制保持一致。

### 需要了解的默认值

- Web 与 API 监听默认 `8080`，内部 Agent 与 HA 监听默认 `8081`。
- 上线初期请保持 `dry_run: true`。此时记录用量并计算成本，但不扣减。
- 余额、用量与 `/metrics` 接口都需要凭据，没有任何一个是匿名开放的。
- 会话 cookie 为 `HttpOnly` 且 `SameSite=Lax`，所有非 GET 的会话请求都必须携带 `X-CSRF-Token`。
- 全部凭据比较均为常量时间比较。

### 已知限制

- HA 是运维自动化，而不是共识协议：没有 quorum、没有自动选主，也没有脑裂保护。防止出现两个同时活跃的主控制器是运维方的责任。
- 本平台不是多租户隔离边界。能够在计算节点提权到 root 的用户，可以绕过节点本地的所有策略执行。
- 迁移只向前执行。回滚意味着恢复备份。

### 从内部部署升级

- 端口从 `60039`/`60040` 调整为 `8080`/`8081`。显式配置 `listen_addr` 与 `internal_listen_addr` 即可保持原值；升级前请同步更新防火墙规则、各节点的 `CONTROLLER_URL` 以及反向代理配置。
- 节点侧辅助工具改为携带运维凭据，从 `GPUOPS_QUERY_TOKEN` 或 `/etc/gpu-ops/query-token` 读取，因为余额接口不再匿名开放。
- `docker-compose.yml` 不再内置数据库密码，请在 `.env` 中提供 `POSTGRES_PASSWORD`。

[3.2.0]: https://github.com/atoz03/Bursar/releases/tag/v3.2.0
